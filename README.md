1. git clone으로 파일을 받으신 후, <code> npm i </code> 를 이용해서 package.json의 라이브러리들을 다운받아주시면 됩니다. 

2. 만약 터미널이 babel-loader을 이유로 구동되지 않는다면 .env파일이 있는지 확인해주시고 안에 SKIP_PREFLIGHT_CHECK=true 를 입력해주세요!

3. 기본적으로 http://localhost:3000/ 을 이용하고 있습니다만 초기 라우팅은 되어있지 않아 뒤에 page1을 붙여주시면 됩니다.  
